## SyväAI website files
This contains all the files used on "chat.syvaai.com".

### GitHub Pages domain behavior
This repository intentionally does **not** use a `CNAME` file so GitHub Pages will not force a custom-domain redirect from the default `*.github.io` URL.
